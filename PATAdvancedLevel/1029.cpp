#include <cstdio>
#include <iostream>
using namespace std;

int A[100010], B[200010];

int main(int argc, char const *argv[]) {
    int n1, n2,ans = 0,count = 0;
    scanf("%d", &n1);
    for (int i = 0; i < n1; i++)
        scanf("%d", &A[i]);
    scanf("%d", &n2);
    int mid = (n1+n2)
    for (int i = 0,j = 0; i < n2; i++) {
        int t;
        scanf("%d", &t);
    
    }

    return 0;
}